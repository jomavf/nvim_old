O = {
    auto_close_tree = 0,
    auto_complete = true,
    colorscheme = 'gruvbox',
    hidden_files = true,
    wrap_lines = false,
    number = true,
    relative_number = true,
    shell = 'zsh',
}

DATA_PATH = vim.fn.stdpath('data')
CACHE_PATH = vim.fn.stdpath('cache')
